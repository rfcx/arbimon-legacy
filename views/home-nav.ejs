<nav class="navbar navbar-default" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button ng-init="collapsedMenu = true" ng-click="collapsedMenu = !collapsedMenu" type="button" class="navbar-toggle collapsed">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        <% if(user.isAnonymousGuest) { %>
            <a href="/" class="navbar-brand">
                <img src="/images/logo.svg">
            </a>
        <% } else { %>
            <a href="/home" class="navbar-brand">
                <img src="/images/logo.svg">
            </a>
        <% } %>
        </div>
        <div class="navbar-collapse" ng-class="{'collapse': collapsedMenu }">
            <ul class="nav navbar-nav">
            <% if(user.isSuper) { %>
                    <li><a href="/admin/"><i class="fa fa-cogs"></i> Admin</a></li>
            <% } %>
                <li class="hide-tooltip-lg" tooltip-placement="bottom" tooltip="Forum">
                    <a target="_blank" href="https://forum.sieve-analytics.com/arbimon/">
                        <i class="fa fa-comments-o"></i>
                        <span class="visible-lg-inline visible-xs-inline">Forum</span>
                    </a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <% include user-dropdown %>
                
                <% include help-dropdown %>
            </ul>
        </div>
    </div>
</nav>
